# La función int() toma un argumento (por ejemplo, 
# una cadena: int(string)) e intenta convertirlo a un valor 
# entero; si llegase a fallar, el programa entero fallará también 
# (existe una manera de solucionar esto, se explicará mas adelante).
# La función float() toma un argumento (por ejemplo, una cadena: 
# float(string)) e intenta convertirlo a flotante (el resto es lo mismo).